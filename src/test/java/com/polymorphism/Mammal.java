package com.polymorphism;

public interface Mammal {
	
	//interface is a relationship
	//interface (multiple inheritance interface)
	//properties
	//set all proprties and variables and 
	//implement into a class
	//abstract class and constant variable
	//by default all methods inside an interface are abstract methods
	//you cannot create an object but you can take help from class in order to create an object 
	//in interface there are no method body.
	//Only methods -- in interface
	
	public void legs (int count);
	
	public void eat();
	}

